<section class="talkers">
    <ul>
        {% for nerd in weeklyNerdData %}
            <li>
                <figure>
                    <img src="{% if nerd.image.src %}{{ nerd.image.src }}{% else %}/images/blank.png{% endif %}" alt="{{ nerd.image.alt | default: 'Afbeelding niet beschikbaar' }}">
                    <figcaption>
                        <h2>{{ nerd.intro }}</h2>
                    </figcaption>
                </figure>
                <a href="/weekly-nerd/{{ nerd.id }}">Lees meer</a>    
            </li>
        {% endfor %}
    </ul>
</section>
