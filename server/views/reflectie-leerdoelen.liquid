{% layout "server/layouts/base.liquid" %}

{% block content %}
<div class="blog-wrapper">
  <h1>{{ title  }}</h1>

  <ul>
    {% for doel in leerdoelen %}
      <li>
        <h2>{{ doel.naam }}</h2>
        <p>{{ doel.wat }}</p>
        <p>{{ doel.waarom }}</p>
        <p>{{ doel.hoe }}</p>
        <p>{{ doel.tevreden }}</p>
      </li>
    {% endfor %}
  </ul>
  </div>
{% endblock %}
